import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
  primary: { main: "#FFA000", contrastText: "#000000" },
  secondary: { main: "#FFE082" }
};
const themeName = "Orange Peel Salomie Jackal";

export default createMuiTheme({ palette, themeName });
